#!/bin/sh

RECIPIENTS='colin@pythontech.co.uk'

tmp=/tmp/riverlevels.tmp

python2.6 /site/pythontech/ptapps/riverlevels.py update > "$tmp" 2>&1
if [ -s "$tmp" ]; then
    (
	echo "To: $RECIPIENTS"
	echo "Subject: River level changes"
	echo ""
	cat "$tmp"
    ) | /usr/sbin/sendmail -t -oi
fi
